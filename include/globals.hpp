#pragma once

// Disperse these into logical locations

constexpr int tile_size = 16;

enum class direction { RIGHT, LEFT, UP, DOWN };

constexpr int window_width = 640;

constexpr int window_height = 480;
